<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <link rel="icon" type="image/svg+xml" href="/Portfolio/favicon.svg" />
        <title>Portfolio</title>
    </head>
    <body class="bg-zinc-950 font-mono text-zinc-50">
        <slot />
    </body>
    <div
        class="hidden bg-blue-500 bg-green-500 bg-purple-500 bg-red-500 bg-teal-500 bg-yellow-500 hover:bg-blue-500 hover:bg-green-500 hover:bg-purple-500 hover:bg-red-500 hover:bg-teal-500 hover:bg-yellow-500"
    >
    </div>
</html>

<script>
    // Setup GSAP plugins
    import { gsap } from "gsap";
    import { MotionPathPlugin } from "gsap/MotionPathPlugin";
    import { TextPlugin } from "gsap/TextPlugin";
    import { ScrollTrigger } from "gsap/ScrollTrigger";
    import { useGSAP } from "@gsap/react";

    gsap.registerPlugin(MotionPathPlugin, TextPlugin, ScrollTrigger, useGSAP);
</script>
